import React from 'react';
import ReactDOM from 'react-dom/client';
import AllpaPrototype from './product.jsx';
import './ui-kit.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AllpaPrototype />
  </React.StrictMode>
);
